# summary

Check which OmniScripts do not have an active version.

# examples

- Check active OmniScripts:

  <%= config.bin %> <%= command.id %> --target-org myOrg@example.com -p cmt

# flags.package.summary

Vlocity Package Type (ins or cmt).
