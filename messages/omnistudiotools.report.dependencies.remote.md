# summary

Get dependency report for a remote environment.

# examples

- Scan remote org:

  <%= config.bin %> <%= command.id %> --target-org SIT -p cmt

# flags.package.summary

Vlocity Package Type (ins or cmt).
