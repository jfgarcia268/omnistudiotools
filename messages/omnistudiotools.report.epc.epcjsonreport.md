# summary

Generate CSV reports from EPC JSON field data.

# examples

- Generate EPC report:

  <%= config.bin %> <%= command.id %> --target-org myOrg@example.com -p cmt -d data.yaml

# flags.package.summary

Vlocity Package Type (ins, cmt, or omnistudio).

# flags.datafile.summary

YAML data definition file.

# flags.separator.summary

CSV separator character.
