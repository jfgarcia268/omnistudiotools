# summary

Delete saved (in-progress) OmniScript instances.

# examples

- Delete saved OmniScript instances with cmt package:

  <%= config.bin %> <%= command.id %> --target-org myOrg@example.com -p cmt

- Delete saved OmniScript instances with ins package:

  <%= config.bin %> <%= command.id %> --target-org myOrg@example.com --package ins

# flags.package.summary

Vlocity Package Type (ins or cmt).
