# summary

Create mock LWC bundles for missing OmniScripts.

# examples

- Create mock LWCs:
  <%= config.bin %> <%= command.id %> --target-org myOrg@example.com -d vlocity

# flags.package.summary

Vlocity Package Type (ins, cmt, or omnistudio).

# flags.datapacksfolder.summary

DataPacks folder of OmniScripts to compare.
